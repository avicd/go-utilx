package logx

import (
	"testing"
)

func TestFileAppender_Write(t *testing.T) {
	//ap := &FileAppender{Name: "test-log", Cycle: Second, Split: Split{
	//	DEBUG: true,
	//}}
	//for i := 0; i < 10; i++ {
	//	for j := 0; j < 1000; j++ {
	//		go func(a1, a2 int) {
	//			ap.Write(a2%5+1, "This is a Year test ", a1, ":", a2)
	//		}(i, j)
	//	}
	//	time.Sleep(time.Second)
	//}
	//time.Sleep(time.Second)
	//ap.Cycle = Month
	//for i := 1; i < int(OFF); i++ {
	//	ap.Write(Level(i), "This is a Month test")
	//}
	//
	//ap.Cycle = Week
	//for i := 1; i < int(OFF); i++ {
	//	ap.Write(Level(i), "This is a Week test")
	//}
	//
	//ap.Cycle = Day
	//for i := 1; i < int(OFF); i++ {
	//	ap.Write(Level(i), "This is a Day test")
	//}
	//
	//ap.Cycle = Hour
	//for i := 1; i < int(OFF); i++ {
	//	ap.Write(Level(i), "This is a Hour test")
	//}
	//
	//ap.Cycle = Minute
	//for i := 1; i < int(OFF); i++ {
	//	ap.Write(Level(i), "This is a Minute test")
	//}
	//
	//ap.Cycle = Hour
	//ap.Split = map[Level]bool{
	//	WARN:  true,
	//	ERROR: true,
	//	FATAL: true,
	//}
	//for i := 1; i < int(OFF); i++ {
	//	ap.Write(Level(i), "This is a Split test")
	//}
}
